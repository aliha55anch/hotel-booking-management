# REQUIRED at build time in production. Full URL of the deployed backend API
# (no trailing slash). Example: VITE_API_URL=https://your-api.onrender.com/api
VITE_API_URL=

# Stripe publishable key (pk_test_... / pk_live_...) — required for card payments.
VITE_STRIPE_PUBLISHABLE_KEY=
